import React from 'react';
import classes from './Request.module.css';

const Request = (props) => {

    return (
        <div>
            <h1>{props.name}</h1>
        </div>
    )
}

export default Request;